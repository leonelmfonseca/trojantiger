Set-Location "C:\path\to\root"; Get-ChildItem -Directory | Where-Object { Test-Path (Join-Path $_.FullName ".git") } | ForEach-Object {
  Push-Location $_.FullName
  try {
    $branch = git branch --show-current
    Write-Output "$($_.Name): $branch"
  } finally {
    Pop-Location
  }
}
